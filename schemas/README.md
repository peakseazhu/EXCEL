# Schema Files

Populate `columns` with ordered column definitions for each table. Example:

```json
{
  "table": "targets_a",
  "columns": [
    {"name": "province", "type": "VARCHAR"},
    {"name": "target_value", "type": "DOUBLE"}
  ]
}
```